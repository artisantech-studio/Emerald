# PATTERN OF THE HOUSE (.state.md)

## 1. Directory Tree
artisan-trust/
├── .env                  # Dev/test defaults (gitignored)
├── .env.prod.template    # Production secrets template
├── .gitignore            # Prevents .env/.env.prod from VCS
├── .state.md
├── docker-compose.yml    # Production: env_file: .env.prod, no bind mount
├── Dockerfile            # Production: gunicorn + non-root user
├── gunicorn_conf.py      # Gunicorn process manager config
├── deploy.sh             # One-command production deployment
├── requirements.txt
├── app/
│   ├── __init__.py
│   ├── core/
│   │   ├── __init__.py
│   │   ├── config.py       # No hardcoded secrets — required fields
│   │   ├── database.py
│   │   ├── plumbline.py
│   │   └── auditor.py
│   └── api/
│       ├── __init__.py
│       ├── middleware.py    # HMAC → HTTP 401
│       └── routes.py
└── tests/
    ├── __init__.py
    ├── test_plumbline.py
    ├── test_gateway.py
    ├── test_auditor.py
    ├── test_webhook_forgery.py     # Ward 3: Forgery attack verification
    └── stress_test_concurrency.py  # Ward 1: 50-concurrent request stress test

## 2. Active Plumbline Schema (PostgreSQL)
- Table: `merkle_ledger`
  - `id`: SERIAL PRIMARY KEY
  - `parent_hash`: TEXT REFERENCES merkle_ledger(current_hash) (NULL for Genesis only)
  - `transaction_payload`: JSONB NOT NULL
  - `timestamp`: TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP
  - `current_hash`: TEXT UNIQUE NOT NULL

## 3. Active Ward & Task Status
- [x] Architecture & Specification Review
- [x] Ward 1: Cryptographic Merkle Core (`plumbline.py`)
- [x] Ward 2: Database Initialization & Isolation (`config.py`, `database.py`, `Dockerfile`, `docker-compose.yml`)
- [x] Ward 3: Pytest Plumbline Test Harness (`tests/test_plumbline.py`)
- [x] Ward 4: WhatsApp Token Signer & Webhook Gateway (`app/api/routes.py`, `tests/test_gateway.py`)
- [x] Ward 5: Meta HMAC-SHA256 Perimeter Middleware (`middleware.py`, `tests/test_gateway.py`)
- [x] Ward 6: Independent Ledger Auditor CLI (`auditor.py`, `tests/test_auditor.py`)
- [x] Ward 7: Live Stack Spin-up & Live Ledger Audit Verification

## 4. Production Hardening Status
- [x] Ward 1: Concurrency Stress Test (50-concurrent FOR UPDATE lock verification)
- [x] Ward 2: Gunicorn + UvicornWorker process manager (4 workers, 120s timeout)
- [x] Ward 3: HMAC Perimeter → HTTP 401, forgery test script
- [x] Ward 4: Secrets externalized, config.py requires all fields, .env.prod.template
- [x] Ward 5: deploy.sh with 8-step validated deployment + verify-chain

## 5. Immutable Constraints
- Hash Algorithm: SHA-256 (Canonical JSON serialization with `sort_keys=True`)
- Plumbline Rule: Every block strictly anchors to `parent_hash`
- DB Concurrency: Strict row-level lock on tail block during append

## Last Successful Validation
- 12/12 Python files pass syntax validation
- gunicorn_conf.py validates: workers=4, UvicornWorker, timeout=120
- deploy.sh passes bash -n syntax check
- All test assertions updated for 401 (was 403)
